# Windsurf AI Coding Rules for tvstcms
# These rules ensure consistency across all AI-assisted edits

## Language & Framework
- Python 3.14
- Flask 3.x web framework
- SQLite database

## Code Style
- Follow PEP8 style guide strictly
- 4-space indentation (no tabs)
- Single quotes for strings (unless string contains single quote)
- Maximum line length: 120 characters
- Remove trailing whitespace
- Two blank lines between top-level functions/classes
- One blank line between class methods

## Import Organization
- Group imports in this order:
  1. Standard library imports
  2. Third-party imports
  3. Local application imports
- One blank line between each group
- Sort alphabetically within each group
- No unused imports

## Documentation
- All route functions MUST have docstrings
- All service classes MUST have docstrings
- All major helper functions MUST have docstrings
- Use clear, concise English
- No commented-out dead code
- Remove completed TODO/FIXME comments

## Naming Conventions
- Variables and functions: snake_case
- Classes: PascalCase
- Constants: UPPER_SNAKE_CASE
- Private methods: _leading_underscore
- Do NOT rename public API endpoints or database columns

## Error Handling
- Every route MUST have try/except blocks
- Never use bare except: - always specify exception type
- Always log errors: logger.error(f'Error message: {e}')
- Return consistent error format: {'success': False, 'error': str(e)}
- No silent failures

## API Response Format
- Success responses: {'success': True, 'data': {...}}
- Error responses: {'success': False, 'error': 'error message'}
- Always return appropriate HTTP status codes
- Use 200 for success, 400 for client errors, 500 for server errors

## Logging
- Use module-level logger: logger = logging.getLogger(__name__)
- NEVER use print() for logging
- Log levels: DEBUG for development, INFO for normal operations, WARNING for issues, ERROR for failures
- Include context in log messages

## Security
- NEVER use f-strings or string concatenation in SQL queries
- Always use parameterized queries: cursor.execute("SELECT * FROM table WHERE id = ?", (id,))
- No hardcoded secrets, API keys, or passwords
- Store sensitive config in environment variables
- Validate and sanitize all user inputs

## Database
- Always use context managers or explicit close()
- Always use parameterized queries
- Handle database errors gracefully
- Use transactions for multi-step operations

## Configuration
- No magic numbers in business logic
- Define constants at top of file: UPPER_SNAKE_CASE
- Move configurable values to config.py
- Use environment variables for deployment-specific settings

## Flask Blueprints
- One blueprint per file
- Register all blueprints in app/__init__.py
- Use url_prefix for API routes
- Group related routes in same blueprint

## File Organization
- Models in app/models/
- Routes in app/routes/
- Services in app/services/
- Utilities in app/utils/
- Configuration in app/config.py
- Scripts in scripts/production/ or scripts/testing/

## Testing
- Write tests for critical business logic
- Use pytest framework
- Mock external dependencies
- Test error cases, not just happy paths

## Git Practices
- Commit messages: present tense, imperative mood
- Keep commits focused and atomic
- Don't commit sensitive data, logs, or cache files
- Update .gitignore as needed

## UI & Design Standards

### Framework & Libraries
- Bootstrap 5.3.x is the UI framework - use it correctly and consistently
- Bootstrap Icons (bi-*) for all icons - do not introduce other icon libraries
- Follow Tabler.io admin dashboard design patterns for inspiration
- Reference: https://tabler.io/preview for visual style guidance

### Mobile-First Rules
- ALL column classes must include col-12 for mobile: col-12 col-md-6, col-12 col-md-3 etc
- Minimum touch target size: 44px height for all buttons and interactive elements
- Font size minimum 16px on inputs to prevent iOS auto-zoom
- Modals must be full-screen on mobile (max-width: 768px)
- Nav tabs/pills must scroll horizontally on mobile, never wrap to multiple lines
- Bottom navigation bar exists for mobile - keep it updated when adding new sections

### Component Standards
- Cards use the settings-card class for settings pages
- Summary/stat cards use col-12 col-md-3 grid layout
- Forms: labels above inputs, full-width on mobile, grouped in row g-3
- Tables: always wrapped in table-responsive, provide card view for mobile
- Buttons: primary actions full-width on mobile, use btn-lg for main CTAs
- Dropdowns: touch-friendly padding (min 12px vertical) on mobile

### Colour & Theming
- Dark mode support via data-bs-theme attribute
- Use CSS variables (--bs-*) not hardcoded hex values
- Success: green, Warning: yellow, Error/Danger: red, Info: blue
- Consistent with existing unified-styles.css

### Notifications & Feedback
- Always show success/error notifications for user actions
- Use the existing showNotification() pattern
- Loading states on all async buttons (spinner + disabled)
- Never leave the user wondering if their action worked

### Accessibility
- All images need alt text
- Form inputs need associated labels
- Colour is never the only indicator of status (add icons/text too)
- ARIA labels on icon-only buttons

### Code Quality for UI
- No inline styles - use CSS classes
- No !important unless absolutely necessary and commented why
- CSS goes in unified-styles.css or page-specific CSS files
- JavaScript UI code follows existing patterns in base.js
